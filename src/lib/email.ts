// Minimal stub for sendEmail to fix build errors. Replace with real implementation if needed.
export async function sendEmail({ to, subject, body }: { to: string; subject: string; body: string }) {
  // No-op stub
  return Promise.resolve();
}
